/**
 * test
 */
const Common = require(__SCRIPTS_DIR__ + "Scripts/_lib/Common");
const View = require(__DIR__ + "include_view");

Common.initConsoleLog(__FILE__, true);

let param = {}
param = View.get_default_parameter(param)
console.log(`Default: ${JSON.stringify(param)}`)

param = View.get_user_parameter(__FILE__, param)
console.log(`With filter: ${JSON.stringify(param)}`)

View.generate_view(param);
Common.finishConsoleLog();