const { doEachRel, ortho } = require(__SCRIPTS_DIR__ + "Scripts/_lib/doEach");
doEachRel(ortho)(selection); // make all existing visual relationships orthogonal (especially after moving an object in a view)