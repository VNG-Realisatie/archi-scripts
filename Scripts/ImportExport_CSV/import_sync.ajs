/**
 * import_sync
 *   Import all objects from a CSV file.
 *   Track synchronization status by marking created, updated, and deleted items.
 */

const Common = require(__SCRIPTS_DIR__ + "Scripts/_lib/Common.js");
// check_JS_Engine(JS_ENGINE_GRAALVM)

const Import = require(__DIR__ + "include_import.js");

Common.initConsoleLog(__FILE__,true);

Import.importObjects(null, true);

Common.finishConsoleLog();
