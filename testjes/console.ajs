load(__DIR__ + "../Common/includeCommon.ajs");

_console_info  = true;
_console_debug = true;
_console_stack = true;
// Show output in the console
initConsoleLog(__FILE__, true)

// info(`info callee.name`);
debug("Message to debug")

level1();

finishConsoleLog();

function level1() {
	stack(`Message to stack`);
}