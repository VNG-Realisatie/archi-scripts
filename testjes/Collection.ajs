var selectedElement = $(selection).filter("concept").first();
var i=0;

i++
console.log(`\nselectedElement (i=${i}) : ${selectedElement}`)

Sel = $(selectedElement.view).not($(selection))
Sel.find(selectedElement.type).forEach(function(occurrence) {
	console.log(`occurrence ${i} : ${occurrence}`)
	i++
})

console.log(`${i} elements of type ${selectedElement.type} in view "${selectedElement.view.name}"`)
	