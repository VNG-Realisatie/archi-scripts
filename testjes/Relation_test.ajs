console.log("===========");

objecten = $(".Hoog");
console.log(">> Objecten: " + objecten);

objecten = $("#Id-bd8ae707b267c0d705f8ecea4662646f");
console.log(">> Objecten: " + objecten);

/*
element = model.createElement("application-function", "Testname");
console.log(">> element: " + element);
console.log(">> element.id: " + element.id);
*/

function anonymize() {
    $("element").each(function(element) {
        properties = $(element).prop();
        if (properties.length > 0) {
            properties.forEach(function(prop) {
                element.removeProp(prop);
			}); // each property
        }
    }); // each element
} 

function ElementPropertyFind(pProperty, pValue) {
	var selectedElement = null;
    $("element").each(function(element) {
//		console.log("> Element : " + element);
		if (element.prop(pProperty) == pValue) {
			debug?console.log("Element found : " + element):true;
			selectedElement = element;
		}
	}); // each element
	return selectedElement;
} 

var debug = false;
// Select element with GEMMA Object ID
console.log("====== ElementPropertyFind ====== " );
selectedElement = ElementPropertyFind("Object ID", "dd2a23f0-2f64-463a-8bd1-170a04ca20fa");
console.log("Element found : " + selectedElement);
