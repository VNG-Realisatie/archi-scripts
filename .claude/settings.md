# Project Claude settings

- When editing a Scripts subfolder, check for a CLAUDE.md in that subfolder
- Preset files: `user_parameter/*.json` — plain JSON, no module.exports
- Do not create new CLAUDE.md files without explicit request
