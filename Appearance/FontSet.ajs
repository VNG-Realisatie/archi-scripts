/**
 * FontSet
 *   Set the font to given fixed type and size
 *
 */
load(__DIR__ + "../_lib/Common.js");
load(__DIR__ + "../_lib/ApplyToSelection.js");

initConsoleLog(__FILE__);

applyToDiagramContent($(selection), FontSet);

finishConsoleLog();

/********************************************
 * function definitions
 *******************************************/
function FontSet(pObject) {
	pObject.fontName = "Arial";
	pObject.fontSize = 12;
}
