/**
 * export_element
 * 	export selected elements to a CSV file
 * 	if a conainer (a view or a folder) is selected, all elements in the container are exported
 */

load(__DIR__ + "../_lib/Common.js");
// checkJavaScriptEngine(ENGINE_GRAAL_VM);

load(__DIR__ + "include_export.js");

initConsoleLog(__FILE__);

exportObjects(OBJECT_TYPE_ELEMENT);

finishConsoleLog();
