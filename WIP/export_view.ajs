/**
 * export_view
 * 	export selected or by selection contained views to a CSV file
 */

load(__DIR__ + "../_lib/Common.js");
// checkJavaScriptEngine(ENGINE_GRAAL_VM);

load(__DIR__ + "include_export.js");

initConsoleLog(__FILE__);

exportObjects(OBJECT_TYPE_VIEW);

finishConsoleLog();
