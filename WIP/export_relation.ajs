/**
 * export_relation
 * 	export selected or by selection contained relations to a CSV file
 */

load(__DIR__ + "../_lib/Common.js");
// checkJavaScriptEngine(ENGINE_GRAAL_VM);

load(__DIR__ + "include_export.js");

initConsoleLog(__FILE__);

exportObjects(OBJECT_TYPE_RELATION);

finishConsoleLog();
